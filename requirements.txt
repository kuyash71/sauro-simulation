# SAURO Python dependencies (pip)
#
# IMPORTANT:
# ROS 2 Jazzy packages must be installed via apt, not pip:
# - rclpy
# - sensor_msgs
# - cv_bridge
# - OpenCV system bindings (python3-opencv)
#
# This file is intentionally limited to project-side Python packages.

# Core utilities
numpy>=1.26,<3.0
PyYAML>=6.0,<7.0

# MAVLink Python interface for mission/control scripts
pymavlink>=2.4.41,<3.0

# Optional but recommended for local tests
pytest>=8.0,<9.0
